# Used by `yarn start:staging`. VITE_ATLAS_GRAPHQL_URL is deliberately unset so the
# environment picker offers only Staging and Production, and starts on Staging.
VITE_WALLET_CONNECT_PROJECT_ID=37bbe7c17829c142d9439aef7657adca

# The dev burner has no role on staging; sign in with a real admin wallet instead.
VITE_DEV_BURNER_PRIVATE_KEY=
